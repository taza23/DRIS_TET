name: AUTO_RESTART_STREAM
on:
  workflow_dispatch:
    inputs:
      video_url:
        description: 'رابط m3u8'
        required: true
      fb_key:
        description: 'Stream Key'
        required: true

jobs:
  streaming:
    runs-on: ubuntu-latest
    steps:
      - name: Install FFmpeg & Fonts
        run: sudo apt update && sudo apt install ffmpeg fonts-freefont-ttf -y

      - name: Infinite Stream Loop
        run: |
          LOGO=""
          
          echo "🚀 بدات عملية البث الذكية مع اللوجو والنص المتحرك..."
          
          while true; do
            echo "🎬 طلاق البث دابا..."
            
            ffmpeg -re -i "${{ github.event.inputs.video_url }}" -i "$LOGO" \
            -filter_complex "[1:v]scale=100:-1[logo];[0:v][logo]overlay=(W-w)/2:20[v1];[v1]drawtext=fontfile=/usr/share/fonts/truetype/freefont/FreeSansBold.ttf:text=' FOOT LIVE ':fontcolor=white:fontsize=45:x=W-mod(t*150\,W+tw):y=H-th-20:shadowcolor=black:shadowx=2:shadowy=2" \
            -c:v libx264 -preset superfast -b:v 2500k \
            -c:a aac -ar 44100 -f flv \
            "rtmps://live-api-s.facebook.com:443/rtmp/${{ github.event.inputs.fb_key }}"
            
            echo "⚠️ البث طاح! غنعاود نطلقو بعد 5 ثواني..."
            sleep 5
          done
